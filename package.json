{
  "name": "galsaril-api",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev --env dev",
    "deploy": "wrangler deploy",
    "cf-typegen": "wrangler types",
    "setup-password": "node scripts/hash-password.mjs"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250515.0",
    "typescript": "^5.5.2",
    "wrangler": "^3.114.17"
  }
}
